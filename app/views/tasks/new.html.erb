<%= link_to 'Back to List', tasks_path, class: "back-link" %> <br />

<div class="tasks new">
  <h2>Create Task</h2>
  <%= form_for(@task) do |f| %>
    <% # url: task_path(task), method: :post %>
    <%= render partial: "form", locals: { f: f } %>
    <div class="form-actions">
      <%= f.submit "Create Task" %>
    </div>
  <% end %>
</div>
